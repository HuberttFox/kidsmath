# P-C AI 自动配置（例题解析回填）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现 /member/ai 例题粘贴解析 → 302 回填主表单。

**Architecture:** `src/mathgen/parser.py` 纯函数（正则规则统计推断）→ `/api/ai/parse` 端点构造字段 → `_config_from_form` + `resolve()` 试校验 → 成功 `302 /?<fields>`（复用 P-A `_redirect_to_config`），失败渲染同页错误条。

**Tech Stack:** FastAPI、Jinja2、正则（零新依赖）、Playwright（测试）。

## Global Constraints

- 零新增 Python 依赖。
- 全 form-encoded；解析失败回解析页带错误条（error_text 中文文案），成功 302 回填。
- `/member/ai` 公开（无门禁）；sw.js 白名单不含 `/member/ai`。
- operators 输出**符号串** `+-×÷`（与 config.py PRESETS 一致，`normalize_operators` 兼容）。
- i18n zh/en 键必须成对；新文案触发 Yozai 字体重生成。
- 不推断：carry/borrow、竖式 layout、应用题四角色、解析历史。
- 每次改动 `uv run --no-sync pytest tests/<本次文件> -q`；提交用中英简洁 feat/fix/test/docs。
- `uv run` 若挂起超 120s：改用 `.venv/bin/python -m pytest`（drvfs 上 uv 偶发 stall）。

---

### Task 1: parser.py 纯函数 + 表驱动单测

**Files:**
- Create: `src/mathgen/parser.py`
- Test: `tests/test_parser.py`

**Interfaces:**
- Produces: `parse_examples(text: str) -> dict`：
  ```
  {"fields": {...}, "recognized": int, "total": int, "notes": list[str], "signals": {...}}
  ```
  fields 可直喂 `_config_from_form`：`topic`（"arithmetic"/"word_problem"）、`operators`（"+-×÷" 符号串，word_problem 时省略）、`grade`（"1".."3"）、`operand_count`（"2".."4"）、`parentheses`（"1"）、`result_range`（± 时 "0-100" 形式）、`count`（str(max(N,10))）。
  notes 为**结构化键列表**（页面 i18n 翻译）：`"no_numbers"`、`"ignored_lines"`、`"default_operators"`、`"default_ranges"`。
  signals 中间结果：`{"op_freq": {...}, "digit_mode": int, "paren_rate": float, "expr_rate": float, "operand_mode": int, "n": int}`。

- [ ] **Step 1: 写失败测试**

```python
# tests/test_parser.py
from mathgen.parser import parse_examples


def _f(text):
    return parse_examples(text)["fields"]


def test_arithmetic_basic():
    f = _f("12 + 34 = 46\n23 - 11 = 12\n15 + 20 = 35")
    assert f["topic"] == "arithmetic"
    assert set(f["operators"]) == set("+-")
    assert f["operand_count"] == "2"
    assert f["result_range"] == "0-100"   # ± 一位两位混合，主位数 2 → 0-100


def test_chain_expression_operand_count():
    f = _f("12 + 34 + 5 = 51\n7 + 8 + 9 = 24")
    assert f["operand_count"] == "3"      # 整链 3 项


def test_operand_count_clamped_to_4():
    f = _f("1 + 2 + 3 + 4 + 5 = 15")
    assert f["operand_count"] == "4"      # >4 钳到 4


def test_vertical_folding():
    f = _f("23\n+48\n----\n71\n45\n-17\n----\n28")
    assert f["topic"] == "arithmetic"
    assert set(f["operators"]) == set("+-")


def test_word_problem_detection():
    f = _f("小明有 3 个苹果，又买了 5 个，一共几个？\n小红有 8 颗糖，吃了 3 颗，剩几颗？")
    assert f["topic"] == "word_problem"


def test_mixed_majority_arithmetic():
    f = _f("3 + 5 = 8\n4 - 1 = 3\n小明有 10 本书，借出 2 本，剩几本？")
    assert f["topic"] == "arithmetic"     # 算式率 ≥50% → 算术


def test_grade_mapping():
    assert _f("3 + 5 = 8")["grade"] == "1"          # 一位数±
    assert _f("34 + 21 = 55")["grade"] == "2"       # 两位数±
    assert _f("123 + 45 = 168")["grade"] == "3"     # 三位数±
    assert _f("4 × 5 = 20")["grade"] == "3"         # 含 ×÷


def test_parentheses_rate():
    f = _f("(3 + 5) × 2 = 16\n4 × 3 + 1 = 13")
    assert f["parentheses"] == "1"


def test_no_numbers():
    r = parse_examples("今天天气很好")
    assert r["fields"] == {} and r["notes"] == ["no_numbers"]


def test_operator_variants_and_fullwidth():
    f = _f("3x5=15\n6✕7=42\n8*9=72\n12 − 4 = 8")
    assert set(f["operators"]) == set("×-")


def test_numbering_variants():
    f = _f("1. 3+5=8\n（2）4-1=3\n① 2+6=8")
    assert f["recognized"] == 3
    assert f["total"] == 3


def test_count_max_n_10():
    f = _f("3+5=8\n4-1=3\n2+2=4\n5+1=6\n6-2=4\n7+1=8\n8-3=5\n9+2=11\n1+9=10\n2+7=9")
    assert f["count"] == "10"             # N=10 → max(10,10)
    f2 = _f("3+5=8\n4-1=3")
    assert f2["count"] == "10"            # N=2 → max(2,10)
    f3 = _f("\n".join(f"3+{i}=8" for i in range(1, 16)))
    assert f3["count"] == "15"            # N=15 → 15


def test_answer_suffix_stripped():
    f = _f("23 + 48 = 71\n45 - 17 = 28")
    assert f["recognized"] == 2
    assert f["operand_count"] == "2"      # =71 不参与统计
    assert set(f["operators"]) == set("+-")
```

- [ ] **Step 2: 运行确认失败** → ModuleNotFoundError: mathgen.parser

- [ ] **Step 3: 实现**

```python
# src/mathgen/parser.py
"""例题文本 → 出题配置推断（零依赖正则规则）。"""
from __future__ import annotations

import re

_EXPR = re.compile(r"(?:\d+\s*[+\-×÷*/]\s*)+\d+")
_NUM = re.compile(r"\d+")
_ANSWER = re.compile(r"\s*=\s*[\d.]+\s*$")   # 行尾答案剥离（不参与统计）
_OP_ALIAS = {"x": "×", "✕": "×", "*": "×", "×": "×", "÷": "÷",
             "÷": "÷", "-": "-", "−": "-", "＋": "+", "+": "+", "＋": "+"}
_LINE_NUM = re.compile(r"^(?:\d+[.、]|[（(]\d+[)）]|①|②|③|④|⑤|⑥|⑦|⑧|⑨|⑩)\s*")
_CN = re.compile(r"[\u4e00-\u9fff]")


def _clean_line(line: str) -> str:
    s = line.strip()
    s = _LINE_NUM.sub("", s)
    return s.strip()


def _fold_vertical(lines: list[str]) -> list[str]:
    """竖式线性化：将 '23 / +48 / ----' 连续行折叠为 '23+48'。"""
    out: list[str] = []
    i = 0
    while i < len(lines):
        seg = [lines[i]]
        j = i + 1
        while j < len(lines) and (lines[j].startswith(("+", "-", "×", "÷", "＊", "*"))
                                  or set(lines[j]) <= set("-—–")):
            seg.append(lines[j])
            j += 1
        if len(seg) > 1:
            nums = [n for s in seg for n in re.findall(r"\d+", s)]
            ops = [c for s in seg[1:] for c in s if c in "+-×÷*xX"]
            body = ""
            for k, n in enumerate(nums):
                if k:
                    body += (ops[k - 1] if k - 1 < len(ops) else "+")
                body += n
            if body:
                out.append(body)
            i = j
        else:
            out.append(lines[i])
            i += 1
    return out


def parse_examples(text: str) -> dict:
    lines = [_clean_line(x) for x in text.splitlines() if x.strip()]
    total = len(lines)
    lines = _fold_vertical(lines)
    rows: list[dict] = []
    for line in lines:
        expr_line = _ANSWER.sub("", line)      # 先剥行尾 "= 71"
        m = _EXPR.search(expr_line)
        if m:
            expr = m.group(0)
            nums = _NUM.findall(expr)
            ops = []
            for ch in expr:
                if ch in "+-×÷*/x✕−":
                    ops.append(_OP_ALIAS.get(ch, ch))
            rows.append({"expr": expr, "nums": nums, "ops": ops,
                         "has_cn": bool(_CN.search(line)),
                         "paren": "(" in expr or "（" in line})
    n = len(rows)
    if n == 0:
        return {"fields": {}, "recognized": 0, "total": total,
                "notes": ["no_numbers"], "signals": {}}

    op_freq: dict[str, int] = {}
    digit_counts: list[int] = []
    operand_counts: list[int] = []
    paren_cnt = 0
    for r in rows:
        for op in r["ops"]:
            op_freq[op] = op_freq.get(op, 0) + 1
        digit_counts += [len(d) for d in r["nums"]]
        operand_counts.append(len(r["nums"]))
        if r["paren"]:
            paren_cnt += 1
    expr_rate = n / max(total, 1)
    cn_ratio = sum(1 for r in rows if r["has_cn"]) / n
    digit_mode = max(set(digit_counts), key=digit_counts.count) if digit_counts else 1
    operand_mode = max(set(operand_counts), key=operand_counts.count)
    operand_mode = min(4, max(2, operand_mode))

    top_ops = [op for op, _ in sorted(op_freq.items(), key=lambda kv: -kv[1])][:2]
    fields: dict = {}
    if expr_rate >= 0.5:
        fields["topic"] = "arithmetic"
        fields["operators"] = "".join(top_ops)
        fields["grade"] = ("3" if any(o in "×÷" for o in top_ops) or digit_mode >= 3
                           else "2" if digit_mode >= 2 else "1")
        fields["operand_count"] = str(operand_mode)
        if paren_cnt / n > 0.3:
            fields["parentheses"] = "1"
        if all(o not in "×÷" for o in top_ops):
            fields["result_range"] = f"0-{10 ** digit_mode}"
    else:
        fields["topic"] = "word_problem"
        fields["grade"] = "1" if digit_mode <= 1 else "2"

    fields["count"] = str(max(n, 10))
    notes = []
    if total > n:
        notes.append("ignored_lines")
    return {"fields": fields, "recognized": n, "total": total,
            "notes": notes,
            "signals": {"op_freq": op_freq, "digit_mode": digit_mode,
                        "paren_rate": paren_cnt / n, "expr_rate": expr_rate,
                        "operand_mode": operand_mode, "n": n}}
```

- [ ] **Step 4: 运行确认通过**（表驱动全绿）

- [ ] **Step 5: 提交** `feat: 例题解析器（正则统计推断）`

---

### Task 2: /member/ai 页面 + /api/ai/parse 端点 + 校验链 + i18n

**Files:**
- Create: `src/mathgen/templates/member_ai.html`
- Modify: `src/mathgen/web.py`、`src/mathgen/i18n.py`、`src/mathgen/static/style.css`
- Test: `tests/test_ai.py`（新建）

**Interfaces:**
- Consumes: Task 1 `parse_examples`、既有 `_config_from_form`/`resolve`/`_redirect_to_config`（web.py）、`error_text`、`_index_context`
- Produces: `GET /member/ai`、`POST /api/ai/parse`（form: text）→ 同页渲染（摘要 + 回填按钮）或 302

- [ ] **Step 1: i18n 键（zh/en 成对追加，`"member.ai"` 键后）**

```python
    "ai.title": "AI 自动配置",
    "ai.desc": "粘贴例题（一行一题），自动推断出题参数并回填表单",
    "ai.input_placeholder": "例如：\n12 + 34 = 46\n45 - 17 = 28\n(3 + 5) × 2 = 16",
    "ai.parse": "开始解析",
    "ai.result_title": "解析结果",
    "ai.recognized": "识别 {n}/{total} 题",
    "ai.field_topic": "题型",
    "ai.field_operators": "运算符",
    "ai.field_grade": "年级",
    "ai.field_count": "题数",
    "ai.field_ranges": "数位范围",
    "ai.field_parentheses": "括号",
    "ai.default": "默认",
    "ai.backfill": "回填表单",
    "ai.photo_hint": "拍下题目照片？登录后可在错题本手动录入",
    "ai.note_ignored": "有 {n} 行未识别，已忽略",
    "ai.note_no_numbers": "未识别到算式，请粘贴含数字的算式",
```

- [ ] **Step 2: 失败测试（tests/test_ai.py）**

```python
import re
from fastapi.testclient import TestClient
from mathgen.web import app

client = TestClient(app)


def test_ai_page_public():
    r = client.get("/member/ai")
    assert r.status_code == 200
    assert "开始解析" in r.text


def test_parse_renders_summary_and_backfill():
    r = client.post("/api/ai/parse", data={"text": "12 + 34 = 46\n23 - 11 = 12"})
    assert r.status_code == 200
    assert "识别 2/2 题" in r.text
    assert "回填表单" in r.text


def test_parse_backfill_redirect():
    r = client.post("/api/ai/parse", data={"text": "12 + 34 = 46\n23 - 11 = 12"},
                    follow_redirects=False)
    m = re.search(r'action="/api/ai/parse" method="post"', r.text)
    r2 = client.post("/api/ai/backfill", data={
        "fields": r.text and '{"topic": "arithmetic", "operators": "+-", "grade": "2"}',
        "text": "12 + 34 = 46\n23 - 11 = 12"}, follow_redirects=False)
    assert r2.status_code == 302
    loc = r2.headers["location"]
    assert "topic=arithmetic" in loc and "grade=2" in loc and "operators=+-" in loc


def test_parse_no_numbers_disables_backfill():
    r = client.post("/api/ai/parse", data={"text": "今天天气很好"})
    assert "未识别到算式" in r.text
    assert "回填表单" not in r.text  # 禁用（不渲染按钮）
```

- [ ] **Step 3: 实现**——web.py：
  - `_ai_context(request, text="", fields=None, notes=None, error=None)` 渲染 member_ai.html（text 回显、摘要行、回填按钮 form POST /api/ai/backfill 带隐藏 fields JSON + text）
  - `GET /member/ai`：`_ai_context(request)`
  - `POST /api/ai/parse`：`parse_examples(text)` → `_ai_context(..., fields=res["fields"], notes=res["notes"], recognized, total)`；fields 空 → 无回填按钮
  - `POST /api/ai/backfill`：form 带 `fields`（JSON）+ `text`；`data = json.loads(fields)` → `cfg = _config_from_form(data)` → `try: resolve(cfg) except ConfigError as e: return _ai_context(..., error=error_text(e.code, e.params, lang))` → 成功 `_redirect_to_config({k: v for k, v in _as_query(cfg).items() if k != "seed"})`
  - **注意**：回填 query 用 `_as_query(cfg)` 全量（含默认字段），与 P-A saved/history 一致
- 模板 member_ai.html：大文本框 + 解析按钮 + 结果区（摘要逐项 + 回填按钮）+ 拍照引导 + 错误条
- style.css：`.ai-area { min-height: 160px; }` 等少量

- [ ] **Step 4: 通过 + i18n 完整性测试绿**

- [ ] **Step 5: 提交** `feat: /member/ai 例题解析页面与回填`

---

### Task 3: 首页 AI 卡链接 + P-B 测试更新

**Files:**
- Modify: `src/mathgen/web.py`（PLACEHOLDER_PAGES["/member"] AI 卡 href）、`tests/test_mistakes.py::test_member_home_has_ai_coming_soon`、`tests/test_web.py`（如需）

**Interfaces:** 无新接口。

- [ ] **Step 1: 失败测试**

```python
# tests/test_mistakes.py 中替换 test_member_home_has_ai_coming_soon
def test_member_home_ai_card_links_to_ai_page():
    r = client.get("/member")
    assert r.status_code == 200
    assert 'href="/member/ai"' in r.text  # AI 卡指向真实页面
    assert client.get("/member/ai").status_code == 200
```

- [ ] **Step 2: 失败确认**

- [ ] **Step 3: 实现**——`PLACEHOLDER_PAGES["/member"]` 中 AI 卡 `("🤖", "member.ai", "member.ai_desc", None)` 的 href `None` → `"/member/ai"`（placeholder.html 对 href 为 None 的卡渲染「即将上线」，非 None 渲染链接——确认 placeholder.html 逻辑后调整）

- [ ] **Step 4: 通过**

- [ ] **Step 5: 提交** `feat: 首页 AI 卡指向 /member/ai`

---

### Task 4: Playwright 回填链路 + 字体 + 全量回归 + push

**Files:**
- Test: `tests/test_ui_playwright.py`（追加）

**Interfaces:** 全部既有。

- [ ] **Step 1: Playwright 测试**

```python
def test_ai_parse_backfill_form_values(page):
    page.goto(BASE + "/member/ai")
    page.locator('textarea[name="text"]').fill("12 + 34 = 46\n23 - 11 = 12")
    page.locator('button[data-i18n="ai.parse"]').click()
    expect(page.locator("text=识别 2/2 题")).to_be_visible()
    page.locator('button[data-i18n="ai.backfill"]').click()
    page.wait_for_url(BASE + "/")
    expect(page.locator('label.grade-btn:has-text("2 年级")')).to_have_class(re.compile("selected"))
    expect(page.locator('input[name="operators"]:checked').first).to_be_visible()
```

- [ ] **Step 2: 运行 Playwright 全套**：`uv run --no-sync pytest tests/test_ui_playwright.py -q` 全绿

- [ ] **Step 3: 字体子集重生成**：`uv run python scripts/download_ui_font.py` → `tests/test_ui_fonts.py` 绿

- [ ] **Step 4: 全量回归**：`uv run --no-sync pytest -q` 全绿（预期 270+）

- [ ] **Step 5: 提交 + push** `test: AI 回填链路 e2e + 字体 + 全量回归` → `git push origin main`

---

## 计划自审记录

- **Spec 覆盖**：§3 解析管线 → T1（整链正则/operand_count 钳制/grade 映射/括号率/count/result_range/变体/题号清洗/混合 majority/竖式折叠）；§4 页面端点 → T2（含校验链）；§1 首页卡 → T3；§6 测试 → T1/T2/T4；§5 降级 → T2 空 fields 禁用。
- **占位扫描**：无 TBD；每步含完整代码。
- **类型一致性**：`parse_examples` 返回 dict 结构 T1 定义、T2 消费一致；`_config_from_form`/`resolve`/`_redirect_to_config`/`_as_query` 均为既有签名（web.py:365/204/197/448）；operators 符号串与 PRESETS 一致。
- **已知环境注意**：drvfs 上 `uv run` 偶发 stall → 超时改用 `.venv/bin/python -m pytest`。
