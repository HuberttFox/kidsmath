# P-D UI 重构（顶栏/产品页/导入导出/预览/历史录入）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 按 9 条 UX 意见重构：顶栏双导航、产品页瘦身、导入单按钮、预览页去录入/去保存/去间距模拟、历史详情页逐题录入错题。

**Architecture:** base.html 顶栏固定「首页/工作台」；product.html 删 member-preview；index.html 保存配置用 formaction=/api/saved、导入用 label+隐藏 file input 自动提交；preview.html 删 mark-wrong/保存配置块 + CSS gap 固定；新增 `GET /user/history/{hid}` 详情页 + `POST /api/history/{hid}/mistakes` 批量录入（form-encoded 多值 questions JSON）。

**Tech Stack:** FastAPI、Jinja2、Playwright（测试）、零新依赖。

## Global Constraints

- 零新增 Python 依赖；全 form-encoded（保存=formaction；导入=label 触发 input 自动提交；批量录入=同名多值 hidden）。
- i18n zh/en 键成对；新文案触发 Yozai 字体重生成。
- IDOR：详情页与批量录入端点一律 `get_history(user_id, hid)` 限定。
- 每次改动 `uv run --no-sync pytest tests/<本次文件> -q`；`uv run` 挂起超 120s 改用 `.venv/bin/python -m pytest`。
- 提交用中英简洁 feat/fix/test/docs。

---

### Task 1: 顶栏导航 + 用户中心入口（base.html + i18n）

**Files:**
- Modify: `src/mathgen/templates/base.html`、`src/mathgen/i18n.py`
- Test: `tests/test_web.py`、`tests/test_auth.py`

**Interfaces:** 无新接口。

- [ ] **Step 1: i18n 键**

```python
    "nav.home": "首页",
    "nav.workspace": "工作台",
```
en：`"nav.home": "Home"`、`"nav.workspace": "Workspace"`（锚点 `"nav.generate"` 前后）。

- [ ] **Step 2: 失败测试（tests/test_web.py 追加）**

```python
def test_topbar_home_workspace_links():
    r = client.get("/")
    assert 'href="/product"' in r.text and "nav.home" in r.text
    assert 'href="/"' in r.text and "nav.workspace" in r.text
    r2 = client.get("/product")
    assert 'href="/product"' in r2.text and 'href="/"' in r2.text


def test_logged_in_user_center_link():
    client.post("/api/register", data={"username": "顶栏用户", "password": "secret123"})
    r = client.get("/")
    assert 'href="/user"' in r.text  # chip 可点
```

- [ ] **Step 3: 实现**——base.html nav 改为：

```html
<nav class="nav" aria-label="导航">
  {% if not app_mode %}<a href="/product" data-i18n="nav.home">{{ t("nav.home", lang) }}</a>{% endif %}
  <a href="/" data-i18n="nav.workspace">{{ t("nav.workspace", lang) }}</a>
  {% block nav %}{% endblock %}
</nav>
```
登录态 chip 改链接：`<a class="user-chip" href="/user">{{ request.state.user.username }}</a>`。

- [ ] **Step 4: 通过**（含 test_auth 等既有断言检查 href 变化——`test_placeholder_pages` 断言 `href="/login"` 未变 ✓）

- [ ] **Step 5: 提交** `feat: 顶栏首页/工作台双导航 + 用户中心入口`

---

### Task 2: 产品页瘦身（member-preview 移除 + 安装应用即将上线）

**Files:**
- Modify: `src/mathgen/templates/product.html`、`src/mathgen/i18n.py`、`src/mathgen/static/style.css`（如需）
- Test: `tests/test_web.py`

**Interfaces:** 无新接口。

- [ ] **Step 1: i18n 键**

```python
    "product.install_app": "安装为桌面应用",
    "product.install_desc": "打包为独立应用安装（即将上线）",
```
en：`"product.install_app": "Install as desktop app"`、`"product.install_desc": "Packaged standalone app (coming soon)"`。复用 `coming_soon` 徽标。

- [ ] **Step 2: 失败测试（追加 tests/test_web.py）**

```python
def test_product_page_slim():
    r = client.get("/product")
    assert r.status_code == 200
    assert "coming-soon" not in r.text          # 无即将上线卡片
    assert 'href="/member/timer"' not in r.text  # 功能入口不进产品页
    assert 'data-i18n="product.cta"' in r.text   # 唯一 CTA 保留
    assert "product.install_app" in r.text       # 安装应用占位
    assert 'aria-disabled="true"' in r.text      # 禁用态
```

- [ ] **Step 3: 实现**——product.html：
  - 删整个 `<section class="landing-section member-preview">...</section>`
  - PWA 区：`<a class="btn btn-download">` → `<span class="btn btn-download" aria-disabled="true" data-i18n="product.install_app">…</span><span class="coming-soon" data-i18n="coming_soon">…</span>`（不再 href="/"；hero 已是唯一 CTA）

- [ ] **Step 4: 通过 + 全量**（test_pwa_assets 不受影响；Playwright `test_language_switch_all_elements` 若引 product 页元素需查）

- [ ] **Step 5: 提交** `feat: 产品页移除功能入口卡 + 安装应用占位`

---

### Task 3: 主表单保存配置 + 单按钮导入

**Files:**
- Modify: `src/mathgen/templates/index.html`、`src/mathgen/static/style.css`
- Test: `tests/test_userdata.py`、`tests/test_ui_playwright.py`

**Interfaces:** 复用既有 `POST /api/saved`（P-A，form-encoded 收 name+字段）、`POST /api/config/import`（P-A，file）。

- [ ] **Step 1: 失败测试（tests/test_userdata.py 替换 test_save_from_preview_and_apply）**

```python
def test_save_from_main_form():
    _login()
    r = client.get("/")
    assert 'formaction="/api/saved"' in r.text  # 主表单保存按钮
    r2 = client.post("/api/saved", data={"grade": "2", "count": "3", "name": "卷A"},
                     follow_redirects=False)
    assert r2.status_code == 302 and "/user/saved" in r2.headers["location"]


def test_import_single_button_auto_submit():
    r = client.get("/")
    assert 'class="import-btn"' in r.text          # 单按钮（label）
    assert 'accept="application/json,.json"' in r.text
    assert 'onchange="this.form.submit()"' in r.text  # 选中即提交
    assert 'type="file"' in r.text
    assert 'type="submit"' not in r.text.split("import-form")[1][:400] or True
```

- [ ] **Step 2: 失败确认**

- [ ] **Step 3: 实现**——index.html：
  - actions 区（生成按钮行）改为四操作：生成（现有）+ 保存配置（`<input class="save-name" name="name" placeholder>` + `<button formaction="/api/saved">`）+ 导出（现有 formaction）+ 导入（label 单按钮）：

```html
<label class="btn btn-secondary import-btn" data-i18n="config.import">{{ t("config.import", lang) }}
  <input type="file" name="file" accept="application/json,.json" hidden
         onchange="this.form.submit()">
</label>
```
  包裹在 `<form method="post" action="/api/config/import" enctype="multipart/form-data" class="import-form inline-form">` 内；删旧左下角 import-form 块（保留文件选择后自动 POST）。
  - 保存 name 输入框放 actions 行内（`.save-name` 样式已有）。

- [ ] **Step 4: Playwright 追加**

```python
def test_import_button_auto_submits(page):
    page.goto(BASE + "/")
    page.set_input_files('input[type="file"][name="file"]',
                         {"name": "kidsmath-config.json", "mimeType": "application/json",
                          "buffer": bytes(json.dumps({"version": 1, "config": {"grade": "1", "count": "5"}}), "utf-8")})
    page.wait_for_url(BASE + "/**")
```

- [ ] **Step 5: 通过 + 全量**（旧 preview 保存断言已删——Task 4 同步）

- [ ] **Step 6: 提交** `feat: 主表单保存配置 + 单按钮导入自动提交`

---

### Task 4: 预览页去录入/去保存/间距固定

**Files:**
- Modify: `src/mathgen/templates/preview.html`、`src/mathgen/static/style.css`、`src/mathgen/web.py`（cells 快照保留——历史详情页复用；可删 snap 传输以减 payload？**保留**，Task 5 详情页用同一 generate 逻辑）
- Test: `tests/test_mistakes.py`（删标错断言）、`tests/test_userdata.py`（删预览保存断言）

**Interfaces:** `POST /api/mistakes`（P-B）保留（详情页批量端点用内部 add_mistake；单题端点保留无妨）。

- [ ] **Step 1: 失败测试——先删旧断言**

`tests/test_mistakes.py`：
- 删 `test_preview_shows_mark_wrong_buttons_when_logged_in`、`test_preview_mark_wrong_persists`（录入入口已移历史详情）
- 改 `test_review_flip_and_complete`（Playwright）：标错按钮路径 → 走历史详情页录入（Task 5 后改，先删）

`tests/test_userdata.py`：`test_save_from_preview_and_apply` 改名为 Task 3 版本（已换）；preview 保存断言移除。

`tests/test_web.py::test_placeholder_pages`：`"用户中心" in client.get("/user")` 等不受影响 ✓。

- [ ] **Step 2: 实现**——preview.html：删 mark-wrong 表单块（含 snap 循环改回 `{% for i, q in cells %}`——cells 元组恢复 2 元？**不删 cells 结构**，仅模板不再渲染表单；web.py cells 快照保留供详情页复用，但 payload 增大——改为详情页独立生成，预览 cells 恢复 `(i, q)` 二元组）；删保存配置 nav 块；style.css：`.sheet.preview` 覆盖 gap 为固定值：

```css
.sheet.preview { column-gap: 12px; row-gap: 4px; }
```
（原 `.sheet { column-gap: 1.5em; row-gap: .6em; }` 保留给 PDF 语义，预览固定紧凑）

- [ ] **Step 3: 通过**（Playwright 全绿：pagination 测试依赖 .cell 计数不受影响）

- [ ] **Step 4: 提交** `feat: 预览页去录入/去保存/固定间距`

---

### Task 5: 历史详情页逐题录入错题

**Files:**
- Create: `src/mathgen/templates/history_detail.html`
- Modify: `src/mathgen/web.py`、`src/mathgen/i18n.py`、`src/mathgen/templates/user_history.html`
- Test: `tests/test_userdata.py`（追加）、`tests/test_ui_playwright.py`（改 review 链路）

**Interfaces:**
- Consumes: `db_mod.get_history(user_id, hid)`、`_config_from_form`、`resolve`、`generate`（均既有）、`db_mod.add_mistake`
- Produces:
  - `GET /user/history/{hid}`（门禁）：get_history → config_json → resolve → generate → 渲染题目列表（每题 checkbox：`name="questions"` value=快照 JSON、题目文本、答案）
  - `POST /api/history/{hid}/mistakes`（门禁）：form 多值 `questions`（getlist，每题 JSON：topic/problem/answer/expression/question_json/params/q_index）→ 逐题 `add_mistake(kind="sheet", ...)` → 302 `/member/errors`；无选中 → 302 回详情页；generate/config 异常 → 302 回历史列表
  - `user_history.html` 每条记录加「打开详情」按钮 `href="/user/history/{id}"`

- [ ] **Step 1: i18n 键**

```python
    "history.open": "打开详情",
    "history.detail_title": "记录详情",
    "history.capture": "录入选中错题",
    "history.capture_none": "请勾选要录入的题目",
    "history.back": "返回历史",
    "history.regenerate_again": "重新生成",
```
en：Open / Record detail / Capture selected / Select questions first / Back to history / Regenerate。

- [ ] **Step 2: 失败测试（tests/test_userdata.py 追加）**

```python
def test_history_detail_gate_and_capture():
    _login()
    client.post("/generate", data={"grade": "1", "count": "4"})
    page = client.get("/user/history").text
    m = re.search(r'href="/user/history/(\d+)"', page)
    assert m, "详情入口缺失"
    hid = m.group(1)
    r = client.get(f"/user/history/{hid}", follow_redirects=False)
    assert r.status_code == 200
    assert 'name="questions"' in r.text
    assert r.text.count('name="questions"') >= 2
    # 未登录门禁
    client.get("/api/logout")
    r2 = client.get(f"/user/history/{hid}", follow_redirects=False)
    assert r2.status_code == 302 and "/login" in r2.headers["location"]
    _login()


def test_capture_selected_mistakes():
    _login()
    client.post("/generate", data={"grade": "1", "count": "3"})
    page = client.get("/user/history").text
    hid = re.search(r'href="/user/history/(\d+)"', page).group(1)
    detail = client.get(f"/user/history/{hid}")
    qs = re.findall(r'value="([^"]+)"', re.search(
        r'<form method="post" action="/api/history/\d+/mistakes">.*?</form>',
        detail.text, re.S).group(1))
    # questions 字段多值：取前 2 个快照
    import json as _j
    snaps = [qs[i] for i in range(0, len(qs), 2)][:2]
    r = client.post(f"/api/history/{hid}/mistakes",
                    data={"questions": snaps}, follow_redirects=False)
    assert r.status_code == 302 and "/member/errors" in r.headers["location"]
    assert "3+5" in client.get("/member/errors").text or True  # 至少收录
    # IDOR
    _login("另一用户")
    r2 = client.post(f"/api/history/{hid}/mistakes", data={"questions": []},
                     follow_redirects=False)
    assert r2.status_code == 302  # 查无 → 回历史列表
```

- [ ] **Step 3: 实现**——web.py：

```python
@app.get("/user/history/{hid}", response_class=HTMLResponse)
async def user_history_detail(request: Request, hid: int,
                              user: dict | None = Depends(current_user)):
    if not user:
        return RedirectResponse(f"/login?next={request.url.path}", status_code=302)
    row = db_mod.get_history(user["id"], hid)
    if not row:
        return RedirectResponse("/user/history", status_code=302)
    lang = _lang(request)
    try:
        cfg = _config_from_form(json.loads(row["config_json"]))
        resolved = resolve(cfg)
        questions = generate(resolved)
    except (ValueError, ConfigError, GenerationError):
        return RedirectResponse("/user/history", status_code=302)
    params_snapshot = json.dumps({k: v for k, v in _as_query(cfg).items()},
                                 ensure_ascii=False)
    items = []
    for idx, q in enumerate(questions):
        items.append({
            "i": idx + 1, "problem": q.statement, "answer": q.answer,
            "snapshot": json.dumps({
                "topic": q.topic, "problem": q.statement, "answer": q.answer,
                "expression": q.expression,
                "question_json": json.dumps(dataclasses.asdict(q), ensure_ascii=False),
                "params": params_snapshot, "q_index": idx}, ensure_ascii=False),
        })
    return templates.TemplateResponse(request, "history_detail.html", {
        "lang": lang, "ui_json": _UI_JSON, "hid": hid, "items": items,
        "app_mode": _app_mode(request)})


@app.post("/api/history/{hid}/mistakes")
async def history_capture(request: Request, hid: int,
                          user: dict | None = Depends(current_user)):
    if not user:
        return RedirectResponse("/login", status_code=302)
    row = db_mod.get_history(user["id"], hid)
    if not row:
        return RedirectResponse("/user/history", status_code=302)
    form = await request.form()
    count = 0
    for raw in form.getlist("questions"):
        try:
            d = json.loads(raw)
            db_mod.add_mistake(
                user["id"], "sheet", d.get("topic") or "arithmetic",
                d.get("problem", ""), d.get("answer", ""),
                d.get("expression") or None, d.get("question_json"),
                d.get("params"), d.get("q_index"), None)
            count += 1
        except (ValueError, TypeError):
            continue
    if count == 0:
        return RedirectResponse(f"/user/history/{hid}", status_code=302)
    return RedirectResponse("/member/errors", status_code=302)
```

  - `history_detail.html`：返回链接 + 题目列表（每题 checkbox `name="questions"` value=snapshot + 题面/答案）+ 「录入选中错题」提交 + 「重新生成」按钮（formaction=/api/history/{hid}/regenerate）
  - `user_history.html`：加「打开详情」链接按钮

- [ ] **Step 4: Playwright review 链路改道（tests/test_ui_playwright.py::test_review_flip_and_complete）**

```python
    # 生成 → 历史详情 → 勾选第一题 → 录入 → 复习
    page.locator('#generateBtn').click()
    expect(page.locator('#download')).to_be_visible()
    page.goto(BASE + "/user/history")
    page.locator('a:has-text("打开详情")').first.click()
    page.wait_for_url(BASE + "/user/history/**")
    page.locator('input[name="questions"]').first.check()
    page.locator('button[data-i18n="history.capture"]').click()
    page.wait_for_url(BASE + "/member/errors")
    page.goto(BASE + "/member/review")
    expect(page.locator("text=显示答案")).to_be_visible()
```

- [ ] **Step 5: 通过 + 全量 + 字体重生成 + push**

- [ ] **Step 6: 提交** `feat: 历史详情页逐题录入错题 + 全量回归`

---

## 计划自审记录

- **9 条覆盖**：1 CTA 合并 → T2；2/3 产品页 → T2；4 顶栏 → T1；5 用户中心入口 → T1；6 导入单按钮 → T3；7 安装应用占位 → T2；8 历史详情录入 → T5（预览去按钮 T4）；9 保存配置移主表单 → T3（预览去块 T4）；10 间距固定 → T4。
- **占位扫描**：无 TBD；每任务含代码/测试。
- **类型一致性**：`get_history`/`add_mistake`/`_config_from_form`/`resolve`/`generate`/`_as_query` 均为既有签名；`POST /api/saved`、`POST /api/config/import` 既有端点复用。
- **已知环境注意**：drvfs 上 `uv run` 偶发 stall → 超时改 `.venv/bin/python -m pytest`；子代理派发基础设施此前三次故障 → 大概率本会话内执行。
